import { HrefStopPropogationDirective } from './href-stop-propogation.directive';

describe('HrefStopPropogationDirective', () => {
  it('should create an instance', () => {
    const directive = new HrefStopPropogationDirective();
    expect(directive).toBeTruthy();
  });
});
