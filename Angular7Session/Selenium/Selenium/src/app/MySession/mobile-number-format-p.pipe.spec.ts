import { MobileNumberFormatPPipe } from './mobile-number-format-p.pipe';

describe('MobileNumberFormatPPipe', () => {
  it('create an instance', () => {
    const pipe = new MobileNumberFormatPPipe();
    expect(pipe).toBeTruthy();
  });
});
